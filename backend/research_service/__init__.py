"""Extension-owned research package (extensions/plans/CONTRACTS.md C1). Reads the frozen
baseline's public outputs read-only; never patches, imports a private baseline symbol, or
writes into a baseline-owned path. See backend/research_service/_.md.
"""
